<?php
/**
 * app/Controllers/BaseController.php
 * Nothing needed here yet for the dashboard test — extend with shared
 * helpers (redirect(), validate(), json(), etc.) as the app grows.
 */

class BaseController
{
}
